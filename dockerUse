
step 1:  pull docker image ```postgresql``` and pgAdmin
step 2:  ```docker-compose up -d``` (to use in detached mode)
step 3:  open pgAdmin with ```0.0.0.0:8000``` in your respective browser




```
#pd-admin
### username: 1234@admin.com
### password: admin

```
Next steps
1. enter by docker network inspect pgv_default to check postgres_container Ipv4
2. user: postgres
3. password: docker
4. db: postgres

```

```
Note

If stop these services, use docker-compose stop

if start these services, use docker-compose start

if delete these services, use docker-compose down

if delete all docker volumes, use docker volume prune

```
